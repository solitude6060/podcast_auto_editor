# PR #43 — triple-review (2026-05-17)

PR: https://github.com/solitude6060/podcast_auto_editor/pull/43
Base: `dev` (a0f0979) ← Head: `feature/pr-a2-real-podcast-walkthrough` (8c46171)
Reviewers: Gemini 3.1 Pro Preview, MiniMax-M2.7 via claude-mm, Codex via codex-family CLI.

## Triage

| # | Finding | Sources | Calibrated severity | Action |
|---|---|---|---|---|
| 1 | `scripts/walkthrough-real-podcast.sh:54` unguarded `rm -rf "$OUT"` | Codex HIGH + Gemini MED | **HIGH** | Fixed — guard against empty / `/` / `$HOME` / unresolved paths |
| 2 | `podcast_auto_editor/cli.py:307` constructs `raw/<episode_id>.wav` from unvalidated `episode_id`; `--episode-id "../x"` can escape `raw/` and `unlink` an existing file | Codex HIGH (sole) | **HIGH** | Fixed — call `validate_episode_id` before `_copy_real_audio_for_quickstart` |
| 3 | Relative `--audio` / `--out` passed to script break after `cd "$ROOT"` | Gemini HIGH (sole) | **MEDIUM** | Fixed — resolve to absolute before cd |
| 4 | `quickstart` returns exit `0` when quality-gate fallback path is taken; conflates "inspection success" with "fully clean" | MiniMax MED (sole) | **MEDIUM → Skipped** | Plan-file Verification section explicitly documents this as intentional ("completed and produced inspection artifacts. The EP1 smoke WAV failed the existing publish loudness gate"). Stderr warning + runbook record the semantics. Distinct exit code is a UX nicety, not a regression. |
| 5 | `podcast_auto_editor/cli.py:293` lacks `Path.resolve()` canonicalization on real-audio source | Gemini LOW + MiniMax MED + Codex MED | **MEDIUM** | Fixed — three-reviewer agreement, one-line change |
| 6 | Hardlink `→` copy fallback silent | MiniMax LOW (intentional) | LOW | Skipped — intentional design |
| 7 | Env-gated test filename undocumented in test docstring | MiniMax LOW + Codex LOW | LOW | Skipped — documented in runbook + plan |
| 8 | Missing tests for directory input / symlink / path-with-spaces (non-env-gated) | Codex LOW | LOW | Partial — added tests for finding #2 (episode_id traversal); other gaps deferred (cheap to add later, not regression-critical) |

## Severity calibration notes

- **Finding #2 (Codex HIGH sole)** is the standout. Only Codex traced the call sequence from raw `args.episode_id` → `out_root/raw/<episode_id>.wav` → `target.unlink()` and noticed that `validate_episode_id` runs later inside `run_paths()`. Gemini and MiniMax both flagged path-traversal concerns but on the *source* path; only Codex caught the *destination* gap. Calibrated HIGH per Codex's diff-correctness + cross-reference role.
- **Finding #1 (rm -rf)** — Codex HIGH vs Gemini MED. Codex cites the explicit "no rm -rf blast" invariant from the prompt. Final HIGH.
- **Finding #4 (exit code)** — only MiniMax flagged. Plan-file Verification section explicitly documents the chosen semantics. Skipped with rationale (intentional, not regression).
- **Finding #5 (canonicalization)** — three-way agreement but G LOW + M MED + C MED. Final MEDIUM per majority.
- No reviewer hallucinations detected. Codex's `cli.py:307` claim was independently verified by reading the file before dispatching the fix.

## Verdicts

- Gemini: REQUEST CHANGES (1 HIGH, 1 MED, 1 LOW)
- MiniMax: APPROVE (0 HIGH, 2 MED, 2 LOW)
- Codex: REQUEST CHANGES (2 HIGH, 1 MED, 1 LOW)

Net calibrated: 2 HIGH + 2 MED for fix; remaining LOWs skipped with rationale.

---

## Raw reviewer outputs

### Gemini

```
1. Verdict: REQUEST CHANGES

HIGH:
- scripts/walkthrough-real-podcast.sh:49 — relative paths break after `cd "$ROOT"`.
  Fix: resolve $AUDIO / $OUT to absolute before cd.

MEDIUM:
- scripts/walkthrough-real-podcast.sh:54 — `rm -rf "$OUT"` no guardrail.
  Fix: reject empty / "/" / "$HOME" before delete.

LOW:
- podcast_auto_editor/cli.py:294 — Path canonicalization missing.
  Fix: `audio_path = Path(path).resolve()`.

Risk callouts: input validation OK, provider safety verified, quality-gate handling
correct, TDD discipline maintained, no new deps, no real audio committed.
```

### MiniMax

```
Verdict: APPROVE

MEDIUM:
1. cli.py:293-301 — _validate_real_audio_path doesn't canonicalize. Recommend .resolve().
2. cli.py:1044-1064 — exit code conflates "review artifacts written" with "fully clean".
   Suggest distinct exit code for inspection-only fallback path.

LOW:
3. _copy_real_audio_for_quickstart hardlink fallback silently uses shutil.copy2. Correct, undocumented.
4. Env-gated test depends on filename "Untitled_1 #06.wav" — already in runbook, no action needed.

Risk callouts: input validation correctly rejects missing/non-files/non-wav. Provider defaults
verified safe. Quality-gate fallback logic correct, only the exit code is the gap. TDD discipline
maintained. No new deps. No committed real audio.
```

### Codex (gpt-5.5 via codex-family CLI)

```
Verdict: REQUEST CHANGES

HIGH:
- scripts/walkthrough-real-podcast.sh:54 — rm -rf "$OUT" without guard violates "no rm -rf blast" invariant.
  Fix: restrict to repo-local walkthrough root, reject absolute / "..", or use marker-file pattern.
- podcast_auto_editor/cli.py:307 — raw episode_id constructs out_root/raw/<id>.wav before run_paths()
  validates the id. `--episode-id "../x"` escapes raw/ and target.unlink() can remove pre-existing path.
  Fix: call validate_episode_id BEFORE _copy_real_audio_for_quickstart, or derive from validated run_paths.

MEDIUM:
- cli.py:293 — no canonicalization (.resolve(strict=True)) on real-audio source.
  Fix: resolve once after validation, use canonical path for copy/hardlink/probe.
  Add tests for symlinked WAVs + symlink-to-dir rejection.

LOW:
- tests/test_walkthrough_real_audio.py:107,122 — missing tests for directory input, path-with-spaces
  (without env var), symlink behavior, invalid episode_id (#2 above).

Risk callouts: Input validation rejects missing/non-files/non-wav with stderr. Provider safety verified
(no args.real_audio toggles provider). Quality-gate handling correct; exit 0 on inspection-only matches
intent provided stderr is observed. TDD pair present. Runner script lacks rm -rf guard (above). Runbook
clear on deferred providers. No new deps. No committed audio.
```
